## 2024-05-24 - [Skip Link Implementation]
**Learning:** Hugo absURL filter can be tricky with local dev environments when testing custom CSS overrides.
**Action:** Always check hugo.toml for baseURL configuration and consider using relURL if assets are missing locally.
