## 2025-02-27 - [Skip Link Implementation]
**Learning:** `hugo-classic` themes may lack basic accessibility structures like `<main>` or skip links.
**Action:** Always check `layouts/` for semantic HTML structure before adding CSS. Ensuring semantic `<main>` usage also unified the layout width in this case.
