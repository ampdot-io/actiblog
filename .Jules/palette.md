## 2025-05-15 - Initial Setup
**Learning:** This project uses Hugo with a classic theme. It relies on `hugo.toml` for configuration and supports custom CSS via a `custom_css` param.
**Action:** Use `custom_css` to inject accessibility styles without modifying the theme's core CSS.
